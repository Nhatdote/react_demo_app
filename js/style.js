import React from 'react';
import {StyleSheet, Text} from 'react-native';

export default StyleSheet.create({
    noData: {
        fontSize: 24,
        fontWeight: '100',
        color: 'red',
        textAlign: 'center',
        marginTop: 100
    }
});